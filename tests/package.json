{
  "name": "fig-tests",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for Fig — drives the real site through a real browser.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "agent": "node agent/run.mjs",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.19.17",
    "http-server": "^14.1.1",
    "typescript": "^6.0.3"
  }
}
